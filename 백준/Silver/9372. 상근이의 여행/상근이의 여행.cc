#include <bits/stdc++.h>

using namespace std;

int main() {
    int T, N, M;
    
    cin >> T;
    
    while(T--) {
        cin >> N >> M;
        
        for (int i = 0; i < M; i++) {
            int a, b;
            cin >> a >> b;
            
        }
        
        cout << N-1 << '\n';
    }
    
    
}
